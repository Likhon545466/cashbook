name: cashbook
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+4

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  sqflite: ^2.4.3
  path: ^1.9.1
  intl: ^0.20.3
  path_provider: ^2.1.6
  share_plus: ^12.0.2
  local_auth: ^3.0.2
  file_selector: 1.1.0
  dynamic_color: 1.9.0
  pdf: ^3.13.0
  crypto: ^3.0.7
  encrypt: ^5.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
